import { NvBorderColorDirective } from './nv-border-color.directive';

describe('NvBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NvBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
