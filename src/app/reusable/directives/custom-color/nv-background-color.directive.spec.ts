import { NvBackgroundColorDirective } from './nv-background-color.directive';

describe('NvBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NvBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
