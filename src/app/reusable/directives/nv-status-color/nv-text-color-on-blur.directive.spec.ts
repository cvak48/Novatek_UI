import { NvTextColorOnBlurDirective } from './nv-text-color-on-blur.directive';

describe('NvTextColorOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new NvTextColorOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
