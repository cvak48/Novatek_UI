import { NvStyleColorOnBlurDirective } from './nv-style-color-on-blur.directive';

describe('NvStyleColorOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new NvStyleColorOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
