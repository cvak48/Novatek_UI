import { SvgDecoratorDirective } from './svg-decorator.directive';

describe('SvgDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
